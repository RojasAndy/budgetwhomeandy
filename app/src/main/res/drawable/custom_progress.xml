<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <size android:height="32dp" /> <!-- Height of the progress bar -->
            <corners android:radius="9dp" /> <!-- Adjust corner radius as needed -->
            <solid android:color="#CCCBCB" /> <!-- Background color -->
        </shape>
    </item>
    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <size android:height="32dp" /> <!-- Height of the progress bar -->
                <corners android:radius="9dp" /> <!-- Adjust corner radius as needed -->
                <solid android:color="#53BD71" /> <!-- Progress color -->
            </shape>
        </clip>
    </item>
</layer-list>